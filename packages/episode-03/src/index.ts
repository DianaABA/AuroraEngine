/**
 * Episode 03 entry. Export your VN content wiring here.
 */
export const episodeId = "episode-03";
export type EpisodeMeta = { id: string; title?: string };
export const meta: EpisodeMeta = { id: episodeId };
